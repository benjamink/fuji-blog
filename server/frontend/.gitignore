# Vite / React frontend

# Dependencies
node_modules/

# Production build output (served by FastAPI; regenerated by `npm run build`)
dist/

# Local env overrides
.env.local
.env.*.local

# TypeScript incremental build info
*.tsbuildinfo

# Ignore the .htpasswd file in the data directory, which may contain sensitive information
data/.htpasswd
